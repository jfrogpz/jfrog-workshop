{
    "key": "${repo-name}",
    "packageType": "${package-type}",
    "rclass": "${repo-type}",
    "url": "${repo-url}",
    "repoLayoutRef": "${repo-layout}",
    "xrayIndex": "${xray-enable}"
  }
